import React from 'react'

const UnitManager = () => {
  // unitshop
  // faction panels
  // 2 empty panels with dropdowns
  // + on the right side
  // max of 4 panels, + disappears if 4 factions

  return (
    <div>UnitManager</div>
  )
}

export default UnitManager
